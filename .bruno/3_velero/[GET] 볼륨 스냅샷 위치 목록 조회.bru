meta {
  name: 9_볼륨 스냅샷 위치 목록 조회
  type: http
  seq: 9
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/volume-snapshot-locations
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": {
      "kubeconfig": "{{base64_local_kubeconfig}}"
    },
    "minio": {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
    }
  }
}

settings {
  encodeUrl: true
}
