meta {
  name: 12_복구 삭제
  type: http
  seq: 12
}

delete {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/restores/{{restoreName}}
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": {
      "kubeconfig": "{{base64_local_kubeconfig}}"
    },
    "minio": {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
    }
  }
}

settings {
  encodeUrl: true
}
