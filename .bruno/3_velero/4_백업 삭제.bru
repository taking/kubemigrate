meta {
  name: 4_백업 삭제
  type: http
  seq: 6
}

delete {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/backups/{{backupName}}
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": {
      "kubeconfig": "{{base64_local_kubeconfig}}"
    },
    "minio": {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
    }
  }
}

settings {
  encodeUrl: true
}
