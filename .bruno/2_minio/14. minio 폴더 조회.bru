meta {
  name: 14_minio 폴더 조회
  type: http
  seq: 14
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket/folders/test
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
