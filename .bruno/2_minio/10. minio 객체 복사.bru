meta {
  name: 10_minio 객체 복사
  type: http
  seq: 10
}

post {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket/objects/test-file.txt/copy/test-bucket/copy-test-file.txt
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
