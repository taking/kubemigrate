meta {
  name: 4_minio 버킷 목록 조회
  type: http
  seq: 4
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
