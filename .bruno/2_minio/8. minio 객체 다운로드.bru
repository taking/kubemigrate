meta {
  name: 8_minio 객체 다운로드
  type: http
  seq: 8
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket/objects/test-file.txt
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
