meta {
  name: 2_minio 버킷 존재 확인
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url2}}",
      "accessKey": "{{minio_accesskey2}}",
      "secretKey": "{{minio_secretkey2}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
