meta {
  name: 2_minio 버킷 존재 확인
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/velero/status
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false,
      "bucketName": "velero",
      "region": "us-east-1"
  }
}

settings {
  encodeUrl: true
}
