meta {
  name: 5_minio 버킷 삭제
  type: http
  seq: 5
}

delete {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
