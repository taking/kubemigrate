meta {
  name: 12_minio Presigned GET URL 생성
  type: http
  seq: 12
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket/objects/test-file.txt/presigned-get?expiry=1800
  body: json
  auth: inherit
}

params:query {
  expiry: 1800
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
