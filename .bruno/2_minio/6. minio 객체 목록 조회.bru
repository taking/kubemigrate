meta {
  name: 6_minio 객체 목록 조회
  type: http
  seq: 6
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/buckets/test-bucket/objects
  body: json
  auth: inherit
}

body:json {
  {
      "endpoint": "{{minio_url}}",
      "accessKey": "{{minio_accesskey}}",
      "secretKey": "{{minio_secretkey}}",
      "useSSL": false
  }
}

settings {
  encodeUrl: true
}
