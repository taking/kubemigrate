meta {
  name: [GET] Helm 동작 여부 확인
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/health
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
