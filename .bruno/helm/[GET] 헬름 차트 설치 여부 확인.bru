meta {
  name: [GET] 헬름 차트 설치 여부 확인
  type: http
  seq: 9
}

post {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/chart_check
  body: json
  auth: inherit
}

body:json {
  {
    "chartName": "podinfo",
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
