meta {
  name: 2_헬름 차트 존재 확인
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/chart/podinfo/status
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
