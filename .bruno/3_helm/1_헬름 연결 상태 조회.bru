meta {
  name: 1_헬름 연결 상태 조회
  type: http
  seq: 1
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/health
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
