meta {
  name: 6_헬름 차트 삭제
  type: http
  seq: 6
}

delete {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/chart/podinfo?namespace=default&dryrun=true
  body: json
  auth: inherit
}

params:query {
  namespace: default
  dryrun: true
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
