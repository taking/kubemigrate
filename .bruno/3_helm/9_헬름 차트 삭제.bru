meta {
  name: 9_헬름 차트 삭제
  type: http
  seq: 9
}

delete {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/nginx?namespace=default&dryrun=true
  body: json
  auth: inherit
}

params:query {
  namespace: default
  dryrun: true
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}