meta {
  name: [POST] 버킷 확인 및 생성
  type: http
  seq: 2
}

post {
  url: {{host_ip}}:{{host_port}}/api/v1/minio/bucket_check
  body: json
  auth: inherit
}

body:json {
  {
      "useSSL": false,
      "endpoint": "192.168.110.118:9000",
      "accessKey": "admin",
      "secretKey": "password",
      "bucketName": "velero",
      "region": "us-east-1"
  }
}

settings {
  encodeUrl: true
}
