meta {
  name: Get Storage Classes
  type: http
  seq: 9
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/kube/storage-classes
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
