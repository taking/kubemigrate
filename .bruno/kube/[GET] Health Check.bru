meta {
  name: [GET] Health Check
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/kube/health
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
