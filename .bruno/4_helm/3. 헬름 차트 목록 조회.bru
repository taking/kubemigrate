meta {
  name: 3_헬름 차트 목록 조회
  type: http
  seq: 3
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts?namespace=all
  body: json
  auth: inherit
}

params:query {
  namespace: all
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
