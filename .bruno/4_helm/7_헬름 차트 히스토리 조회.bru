meta {
  name: 7_헬름 차트 히스토리 조회
  type: http
  seq: 7
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/nginx/history?namespace=default
  body: json
  auth: inherit
}

params:query {
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}