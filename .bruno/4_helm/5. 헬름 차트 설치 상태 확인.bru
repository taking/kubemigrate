meta {
  name: 5_헬름 차트 설치 상태 확인
  type: http
  seq: 5
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/nginx/status?namespace=default
  body: json
  auth: inherit
}

params:query {
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
