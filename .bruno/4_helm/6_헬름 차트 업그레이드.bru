meta {
  name: 6_헬름 차트 업그레이드
  type: http
  seq: 6
}

put {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/nginx?chartPath=bitnami/nginx&namespace=default
  body: json
  auth: inherit
}

params:query {
  chartPath: bitnami/nginx
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
