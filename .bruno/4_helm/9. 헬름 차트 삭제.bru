meta {
  name: 9_헬름 차트 삭제
  type: http
  seq: 9
}

delete {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/wordpress-test?namespace=wordpress-test
  body: json
  auth: inherit
}

params:query {
  namespace: wordpress-test
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

script:post-response {
  bru.setEnvVar("jobId", res.body.data.job.jobId);
}

settings {
  encodeUrl: true
}
