meta {
  name: 8_헬름 차트 값 조회
  type: http
  seq: 8
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/nginx/values?namespace=default
  body: json
  auth: inherit
}

params:query {
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}