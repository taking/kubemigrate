meta {
  name: 11_헬름 차트 Job 로그 조회
  type: http
  seq: 11
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/helm/charts/logs/{{jobId}}
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
