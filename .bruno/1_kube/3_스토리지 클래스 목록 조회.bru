meta {
  name: 3_스토리지 클래스 목록 조회
  type: http
  seq: 3
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/kube/storage-classes
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
