meta {
  name: 5_스토리지 클래스 단일 조회
  type: http
  seq: 5
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/kubernetes/storage-classes/{{scName}}
  body: json
  auth: inherit
}

params:query {
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
