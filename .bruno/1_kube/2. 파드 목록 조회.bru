meta {
  name: 2_파드 목록 조회
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/kubernetes/pods?namespace=default
  body: json
  auth: inherit
}

params:query {
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
