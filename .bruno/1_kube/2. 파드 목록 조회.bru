meta {
  name: 2_파드 목록 조회
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/kubernetes/pods?namespace=default
  body: json
  auth: inherit
}

params:query {
  namespace: default
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

script:post-response {
  bru.setEnvVar("podName", res.body.data.items[0].metadata.name);
}

settings {
  encodeUrl: true
}
