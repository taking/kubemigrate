meta {
  name: Get Backup List
  type: http
  seq: 2
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/backups
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
