meta {
  name: Get Backup Storage Locations
  type: http
  seq: 5
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/backup-storage-locations
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
