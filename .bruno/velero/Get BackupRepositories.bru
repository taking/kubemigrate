meta {
  name: Get BackupRepositories
  type: http
  seq: 4
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/backup-repositories
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
