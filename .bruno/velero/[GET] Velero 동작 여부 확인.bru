meta {
  name: [GET] Velero 동작 여부 확인
  type: http
  seq: 1
}

get {
  url: {{host_ip}}:{{host_port}}/api/v1/velero/health
  body: json
  auth: inherit
}

body:json {
  {
    "kubeconfig": "{{base64_local_kubeconfig}}"
  }
}

settings {
  encodeUrl: true
}
